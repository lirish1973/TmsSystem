@model TmsSystem.Models.Trip

@{
    ViewData["Title"] = "מחיקת טיול";
}

<style>
    .rtl-text {
        direction: rtl;
        text-align: right;
    }
</style>

<div class="container mt-4">
    <div class="card shadow border-danger">
        <div class="card-header bg-danger text-white">
            <h3 class="rtl-text"><i class="fas fa-exclamation-triangle"></i> מחיקת טיול</h3>
        </div>
        <div class="card-body">
            <div class="alert alert-warning rtl-text">
                <h5><i class="fas fa-exclamation-circle"></i> האם אתה בטוח שברצונך למחוק את הטיול הזה?</h5>
                <p>פעולה זו לא ניתנת לביטול!</p>
            </div>

            <div class="mb-4 rtl-text">
                <h4>@Model.Title</h4>
                <p><strong>תיאור:</strong> @Model.Description</p>
                <p><strong>מספר ימים:</strong> <span class="badge bg-info">@Model.NumberOfDays ימים</span></p>
                <p><strong>תאריך יצירה:</strong> @Model.CreatedAt.ToString("dd/MM/yyyy HH:mm")</p>
            </div>

            <h5 class="rtl-text">ימי הטיול שיימחקו:</h5>
            <ul class="list-group mb-4">
                @foreach (var day in Model.TripDays.OrderBy(d => d.DayNumber))
                {
                    <li class="list-group-item rtl-text">
                        <strong>יום @day.DayNumber:</strong> @day.Title
                        @if (!string.IsNullOrEmpty(day.ImagePath))
                        {
                            <span class="badge bg-secondary">כולל תמונה</span>
                        }
                    </li>
                }
            </ul>

            <form asp-action="Delete" method="post">
                <input type="hidden" asp-for="TripId" />
                <div class="d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-secondary btn-lg">
                        <i class="fas fa-arrow-right"></i> ביטול
                    </a>
                    <button type="submit" class="btn btn-danger btn-lg">
                        <i class="fas fa-trash"></i> אישור מחיקה
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>